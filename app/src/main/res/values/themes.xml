<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DailyTodo" parent="Theme.Material3.DayNight.NoActionBar">
      <item name="colorPrimary">@color/light_yellow</item>
    </style>

    <style name="CustomToolbarTitle" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">30sp</item> <!-- Set your desired title size here -->
        <item name="android:textColor">@color/black</item> <!-- Optional: Set title color -->
    </style>

    <style name="Theme.DailyTodo" parent="Base.Theme.DailyTodo" />
</resources>